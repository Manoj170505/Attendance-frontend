# For local development, leave VITE_API_URL unset.
# Axios will use '/api' (relative) which gets proxied by Vite to the backend.
# The proxy target is configured in vite.config.js.

# Uncomment below for production builds (no proxy):
# VITE_API_URL=https://attendance-backend-production-48ca.up.railway.app
